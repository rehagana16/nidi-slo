export interface dataKabupatenResponse {
  id: string;
  province_id: string;
  name: string;
}

export interface dataKecamatanResponse {
  id: string;
  regency_id: string;
  name: string;
}

export interface dataDesaResponse {
  id: string;
  district_id: string;
  name: string;
}